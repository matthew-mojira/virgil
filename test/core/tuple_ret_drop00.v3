//@execute 0=1; 1=2; 5=6
def triple(x: int) -> (int, int, int) {
	return (x, x + 1, x + 2);
}
def main(a: int) -> int {
	// only the middle component is used; the other two results of the
	// call are never projected and must still be consumed correctly
	return triple(a).1;
}
