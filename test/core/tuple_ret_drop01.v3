//@execute 0=10; 1=11; 5=15
def triple(x: int) -> (int, int, int) {
	return (x, x + 1, x + 2);
}
def main(a: int) -> int {
	// entire multi-value results are discarded; must not corrupt later stack state
	triple(a);
	triple(a + 100);
	return 10 + a;
}
